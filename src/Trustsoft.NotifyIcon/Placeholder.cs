namespace Trustsoft.NotifyIcon;

// Exists only so the three-TFM build, the packaging gate and the public-API gate can be
// proven before the interop core lands. Internal by design so it can never leak into the
// public API surface.
internal static class Placeholder
{
}
